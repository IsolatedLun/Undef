import React from 'react';

const PrimaryNav = () => {
  return(
      <nav className='primary-nav' role='primary nav'>
          <h1 className="nav__title">Undef</h1>
          <div className="nav__user-profile"></div>
      </nav>
  )
};

export default PrimaryNav;
