import { createSlice } from "@reduxjs/toolkit";
import { INF_Video } from "../components/modules/Video";

interface AccessibilitySlice {
    
}

const initialState: AccessibilitySlice = {
    
}

export const accessiblitySlice = createSlice({
    name: 'accessiblity',
    initialState,
    reducers: {
        
    },
    extraReducers: (builder) => {
        
    }
})

export const {  } = accessiblitySlice.actions;
export default accessiblitySlice.reducer;